{
  "name": "evolution-online",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=api",
    "dev:worker": "npm run dev --workspace=worker",
    "dev:docker": "docker compose -f docker-compose.dev.yml up --build",
    "build": "npm run build --workspace=@pilot/shared && npm run build --workspace=@pilot/database && npm run build --workspace=api && npm run build --workspace=worker",
    "lint": "npm run lint --workspace=api",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "npm run migrate:dev --workspace=@pilot/database",
    "db:generate": "npm run generate --workspace=@pilot/database",
    "start": "npm run start --workspace=api"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
