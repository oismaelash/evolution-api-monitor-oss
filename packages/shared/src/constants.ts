/** Consecutive failed health checks before the first restart cycle (PRD §1.4). */
export const FAILURES_BEFORE_RESTART = 3;

/** Single-tenant OSS dashboard user id (seed + API routes). */
export const OSS_USER_ID = 'oss-user-id';
